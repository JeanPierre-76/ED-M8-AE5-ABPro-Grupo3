<%= form_with(model: internacional) do |form| %>
  <% if internacional.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(internacional.errors.count, "error") %> prohibited this internacional from being saved:</h2>

      <ul>
        <% internacional.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :encabezado, style: "display: block" %>
    <%= form.text_field :encabezado %>
  </div>

  <div>
    <%= form.label :cuepor, style: "display: block" %>
    <%= form.text_area :cuepor %>
  </div>

  <div>
    <%= form.label :link, style: "display: block" %>
    <%= form.text_field :link %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
